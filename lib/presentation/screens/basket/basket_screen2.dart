import 'package:flutter/material.dart';

class BasketScreen2 extends StatelessWidget {
  const BasketScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BasketScreen'),
      ),
    );
  }
}
